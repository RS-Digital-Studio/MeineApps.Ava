<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application
    android:allowBackup="false"
    android:usesCleartextTraffic="false"
    android:largeHeap="true"
    android:icon="@mipmap/appicon"
    android:roundIcon="@mipmap/appicon_round"
    android:supportsRtl="false"
    android:label="BomberBlast"
    android:enableOnBackInvokedCallback="false"
    android:theme="@style/MyTheme.NoActionBar">
    <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID"
               android:value="ca-app-pub-2588160251469436~8809763733" />
    <meta-data android:name="com.google.android.gms.games.APP_ID"
               android:value="@string/game_services_project_id" />

    <!-- Firebase Cloud Messaging: Default-Channel + Icon fuer System-Notifications -->
    <meta-data android:name="com.google.firebase.messaging.default_notification_channel_id"
               android:value="bomberblast_liveops" />
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon"
               android:resource="@mipmap/appicon" />

    <!-- FCM Service: erbt von FirebaseMessagingService, empfaengt Push-Messages + Token-Refresh -->
    <service
      android:name="org.rsdigital.bomberblast.BomberBlastMessagingService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>

    <!-- Local-Notification-Receiver: empfaengt AlarmManager-Broadcasts und postet Notifications -->
    <receiver
      android:name="org.rsdigital.bomberblast.NotificationReceiver"
      android:exported="false" />
  </application>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="com.android.vending.BILLING" />
  <!-- Push-Notifications + lokale AlarmManager-Reminder -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- RECEIVE_BOOT_COMPLETED entfernt: es existiert kein Boot-Receiver, der die D1/D3/D7-Reminder
       nach einem Neustart neu plant — die Permission war ungenutzt (Play-Store-Hygiene). -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
