namespace BomberBlast.ViewModels;

/// <summary>
/// Profile-Hub-Tab-System (v2.0.43, Plan — Menu-Redesign).
/// Konsolidiert die fuenf Spieler-Bereiche aus dem alten verstreuten MainMenu.
/// </summary>
public enum ProfileTab
{
    Overview,
    Statistics,
    Achievements,
    Collection,
    Customize
}
