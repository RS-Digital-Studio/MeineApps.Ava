<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:HandwerkerRechner.ViewModels.Premium"
             xmlns:mi="using:Material.Icons.Avalonia"
             xmlns:loc="using:MeineApps.Core.Ava.Localization"
             xmlns:skia="using:Avalonia.Labs.Controls"
             xmlns:i="using:Avalonia.Xaml.Interactivity"
             xmlns:behaviors="using:MeineApps.UI.Behaviors"
             x:Class="HandwerkerRechner.Views.Premium.ScreedView"
             x:DataType="vm:ScreedViewModel"
             x:CompileBindings="True">

  <Grid RowDefinitions="Auto,*,Auto"
        AutomationProperties.AutomationId="Screed_RootGrid">

    <!-- Header: Braun/Steingrau Gradient -->
    <Border Grid.Row="0"
            Background="{DynamicResource HeaderGradientBrush}"
            Padding="16,12,16,20"
            CornerRadius="0,0,24,24"
            AutomationProperties.AutomationId="Screed_HeaderBorder">
      <Grid ColumnDefinitions="Auto,*">
        <Button Command="{Binding GoBackCommand}"
                Background="{DynamicResource HeaderChipBrush}"
                CornerRadius="20"
                Width="40" Height="40"
                Padding="0"
                AutomationProperties.AutomationId="Screed_BtnBack">
          <mi:MaterialIcon Kind="ArrowLeft" Width="22" Height="22" Foreground="White" />
        </Button>
        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12"
                    Margin="12,0,0,0" VerticalAlignment="Center">
          <Border Background="{DynamicResource HeaderChipBrush}" CornerRadius="12" Width="40" Height="40">
            <mi:MaterialIcon Kind="Layers" Width="22" Height="22"
                             Foreground="White"
                             HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <TextBlock Text="{loc:Translate CalcScreed}"
                     FontSize="{StaticResource FontSizeHeadlineSm}" FontWeight="Bold" Foreground="White"
                     VerticalAlignment="Center"
                     AutomationProperties.AutomationId="Screed_TxtTitle" />
        </StackPanel>
      </Grid>
    </Border>

    <!-- Content -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                  AutomationProperties.AutomationId="Screed_ScrollViewer">
      <StackPanel Spacing="16" Margin="16,16,16,200">

        <!-- Input Card -->
        <Border Classes="Card StatsCard"
                AutomationProperties.AutomationId="Screed_CardInput">
          <StackPanel Spacing="12">
            <TextBlock Text="{loc:Translate FloorArea}"
                       FontSize="{StaticResource FontSizeTitleMd}" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       AutomationProperties.AutomationId="Screed_TxtInputHeader" />

            <!-- Bodenfläche -->
            <StackPanel Spacing="4">
              <TextBlock Text="{loc:Translate FloorArea}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBox Text="{Binding FloorArea}"
                       PlaceholderText="m²"
                       AutomationProperties.AutomationId="Screed_TxtBoxFloorArea" />
            </StackPanel>

            <!-- Estrichdicke -->
            <StackPanel Spacing="4">
              <TextBlock Text="{loc:Translate ScreedThickness}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBox Text="{Binding ThicknessCm}"
                       PlaceholderText="cm"
                       AutomationProperties.AutomationId="Screed_TxtBoxThickness" />
            </StackPanel>

            <!-- Estrich-Typ (ComboBox) -->
            <StackPanel Spacing="4">
              <TextBlock Text="{loc:Translate ScreedType}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <ComboBox SelectedIndex="{Binding SelectedScreedType}"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.AutomationId="Screed_CbScreedType">
                <ComboBoxItem Content="{loc:Translate ScreedTypeZement}" />
                <ComboBoxItem Content="{loc:Translate ScreedTypeFlow}" />
                <ComboBoxItem Content="{loc:Translate ScreedTypeAnhydrit}" />
              </ComboBox>
            </StackPanel>

            <!-- Preis pro Sack (optional) -->
            <StackPanel Spacing="4">
              <TextBlock Text="{loc:Translate PricePerBag}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBox Text="{Binding PricePerBag}"
                       PlaceholderText="{loc:Translate CurrencySymbol}"
                       AutomationProperties.AutomationId="Screed_TxtBoxPricePerBag" />
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- Visualisierung -->
        <Border Classes="Card" Padding="0" Height="220" Margin="0,0,0,0"
                IsVisible="{Binding HasResult}" ClipToBounds="True" CornerRadius="12"
                AutomationProperties.AutomationId="Screed_CardVisualization">
          <skia:SKCanvasView x:Name="ScreedCanvas" PaintSurface="OnPaintVisualization"
                             AutomationProperties.AutomationId="Screed_Canvas" />
        </Border>

        <!-- Results Card -->
        <Border Classes="Card Elevated StatsCard"
                IsVisible="{Binding HasResult}"
                AutomationProperties.AutomationId="Screed_CardResult">
          <StackPanel Spacing="10">
            <TextBlock Text="{loc:Translate Result}"
                       FontSize="{StaticResource FontSizeTitleMd}" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryBrush}"
                       AutomationProperties.AutomationId="Screed_TxtResultHeader" />

            <!-- Hero-Value: Säcke benötigt -->
            <Border CornerRadius="12" Padding="16,12"
                    Margin="0,4,0,8"
                    Background="{DynamicResource TileGradientScreedBrush}">
              <StackPanel>
                <TextBlock Text="{loc:Translate ResultBagsNeeded}" FontSize="{StaticResource FontSizeLabelMd}"
                           Foreground="{DynamicResource OnGradientSubtextBrush}" />
                <TextBlock AutomationProperties.AutomationId="Screed_TxtBagsNeeded"
                           Text="{Binding BagsNeededDisplay}" FontSize="{StaticResource FontSizeHeadlineMd}" FontWeight="Bold"
                           Foreground="White">
                  <i:Interaction.Behaviors>
                    <behaviors:CountUpBehavior TargetValue="{Binding Result.BagsNeeded}" Format="F0" Duration="500" />
                  </i:Interaction.Behaviors>
                </TextBlock>
              </StackPanel>
            </Border>

            <!-- Volumen -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate ScreedVolume}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding VolumeDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtVolume" />
            </Grid>

            <!-- Gewicht -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate ScreedWeight}"
                         FontSize="{StaticResource FontSizeBodyMd}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding WeightDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtWeight" />
            </Grid>

            <!-- Trocknungszeit -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate DryingTime}"
                         FontSize="{StaticResource FontSizeBodyMd}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding DryingTimeDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtDryingTime" />
            </Grid>

            <!-- Fläche -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate FloorArea}"
                         FontSize="{StaticResource FontSizeBodySm}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding AreaDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtArea" />
            </Grid>

            <!-- Estrichdicke -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate ScreedThickness}"
                         FontSize="{StaticResource FontSizeBodyMd}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding ThicknessDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtThickness" />
            </Grid>

            <!-- Estrich-Typ -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="{loc:Translate ScreedType}"
                         FontSize="{StaticResource FontSizeBodyMd}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding ScreedTypeDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtScreedType" />
            </Grid>

            <!-- Gesamtkosten (wenn aktiviert) -->
            <Grid ColumnDefinitions="*,Auto"
                  IsVisible="{Binding ShowCost}">
              <TextBlock Grid.Column="0" Text="{loc:Translate TotalCost}"
                         FontSize="{StaticResource FontSizeBodyMd}"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding TotalCostDisplay}"
                         FontSize="{StaticResource FontSizeBodyMd}" FontWeight="SemiBold"
                         Foreground="{DynamicResource PrimaryBrush}"
                         AutomationProperties.AutomationId="Screed_TxtTotalCost" />
            </Grid>

            <!-- Aktions-Buttons: Share + PDF Export -->
            <Grid ColumnDefinitions="*,8,*,8,Auto" Margin="0,4,0,0">
              <Button Grid.Column="0"
                      Command="{Binding ShareResultCommand}"
                      Classes="Outlined"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Center"
                      AutomationProperties.AutomationId="Screed_BtnShare">
                <StackPanel Orientation="Horizontal" Spacing="4">
                  <mi:MaterialIcon Kind="ContentCopy" Width="16" Height="16"
                                   Foreground="{DynamicResource AccentBrush}" />
                  <TextBlock Text="{loc:Translate ShareResult}"
                             FontSize="{StaticResource FontSizeLabelMd}" TextTrimming="CharacterEllipsis"
                             VerticalAlignment="Center" />
                </StackPanel>
              </Button>
              <Button Grid.Column="2"
                      Command="{Binding ExportMaterialListCommand}"
                      Classes="Outlined"
                      IsEnabled="{Binding !IsExporting}"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Center"
                      AutomationProperties.AutomationId="Screed_BtnExport">
                <StackPanel Orientation="Horizontal" Spacing="4">
                  <mi:MaterialIcon Kind="FilePdfBox" Width="16" Height="16"
                                   Foreground="{DynamicResource AccentBrush}" />
                  <TextBlock Text="{loc:Translate ExportMaterialList}"
                             FontSize="{StaticResource FontSizeLabelMd}" TextTrimming="CharacterEllipsis"
                             VerticalAlignment="Center" />
                </StackPanel>
              </Button>
              <Button Grid.Column="4" Command="{Binding ExportCsvCommand}" Classes="Text" Padding="8"
                      AutomationProperties.AutomationId="Screed_BtnExportCsv">
                <mi:MaterialIcon Kind="FileDelimited" Width="20" Height="20"
                                 Foreground="{DynamicResource AccentBrush}" />
              </Button>
            </Grid>
          </StackPanel>
        </Border>

      </StackPanel>
    </ScrollViewer>

    <!-- Buttons -->
    <Border Grid.Row="2" Padding="16,12" Background="{DynamicResource BackgroundBrush}"
            AutomationProperties.AutomationId="Screed_BottomBar">
      <Grid ColumnDefinitions="*,8,*,8,*">
        <Button Grid.Column="0"
                Command="{Binding ResetCommand}"
                Classes="Outlined"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                AutomationProperties.AutomationId="Screed_BtnReset">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="Refresh" Width="16" Height="16"
                             Foreground="{DynamicResource TextSecondaryBrush}" />
            <TextBlock Text="{loc:Translate Reset}"
                       FontSize="{StaticResource FontSizeLabelMd}" TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <Button Grid.Column="2"
                Command="{Binding CalculateCommand}"
                Classes="Primary"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                AutomationProperties.AutomationId="Screed_BtnCalculate">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="Calculator" Width="16" Height="16" />
            <TextBlock Text="{loc:Translate Calculate}"
                       FontSize="{StaticResource FontSizeLabelMd}" TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <Button Grid.Column="4"
                Classes="Secondary"
                Command="{Binding SaveProjectCommand}"
                IsVisible="{Binding HasResult}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                AutomationProperties.AutomationId="Screed_BtnSave">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="ContentSave" Width="16" Height="16" />
            <TextBlock Text="{loc:Translate Save}"
                       FontSize="{StaticResource FontSizeLabelMd}" TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center" />
          </StackPanel>
        </Button>
      </Grid>
    </Border>

    <!-- Save Project Dialog Overlay -->
    <Border Grid.Row="0" Grid.RowSpan="3"
            ZIndex="100"
            Background="{DynamicResource OverlayBrush}"
            IsVisible="{Binding ShowSaveDialog}"
            AutomationProperties.AutomationId="Screed_SaveDialogOverlay">
      <Border Background="{DynamicResource SurfaceBrush}"
              CornerRadius="16"
              Padding="24"
              MaxWidth="340"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              AutomationProperties.AutomationId="Screed_SaveDialogCard">
        <StackPanel Spacing="16">
          <TextBlock Text="{loc:Translate SaveProject}"
                     FontSize="{StaticResource FontSizeTitleSm}" FontWeight="Bold"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     HorizontalAlignment="Center"
                     AutomationProperties.AutomationId="Screed_TxtSaveDialogTitle" />
          <TextBox Text="{Binding SaveProjectName}"
                   PlaceholderText="{loc:Translate ProjectName}"
                   Classes="Outlined"
                   AutomationProperties.AutomationId="Screed_TxtBoxProjectName" />
          <Grid ColumnDefinitions="*,12,*">
            <Button Grid.Column="0"
                    Classes="Outlined"
                    Command="{Binding CancelSaveProjectCommand}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Center"
                    Content="{loc:Translate Cancel}"
                    AutomationProperties.AutomationId="Screed_BtnCancelSave" />
            <Button Grid.Column="2"
                    Classes="Primary"
                    Command="{Binding ConfirmSaveProjectCommand}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Center"
                    Content="{loc:Translate Save}"
                    AutomationProperties.AutomationId="Screed_BtnConfirmSave" />
          </Grid>
        </StackPanel>
      </Border>
    </Border>

  </Grid>

</UserControl>
